# SEDAR+ Data Collection Configuration
# =================================

# SEDAR+ Base Configuration
SEDAR_BASE_URL=https://www.sedarplus.ca

# Supabase Database Configuration (optional - if not provided, data saves locally)
SUPABASE_URL=your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key
SUPABASE_SCHEMA=public
SUPABASE_PASSWORD=SEDanalytics

# Collection Settings
RATE_LIMIT_DELAY=1.0          # Seconds between requests (be respectful to SEDAR servers)
BATCH_SIZE=5000               # Number of records to fetch per request
MAX_RETRIES=3                 # Number of retry attempts for failed requests

# Storage Configuration
DOWNLOAD_DIR=./data/pdfs      # Where to store downloaded PDF files
CACHE_DIR=./data/cache        # Where to store cached CSV data

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO